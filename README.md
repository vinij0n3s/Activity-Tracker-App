
  # Activity Tracker App

  This is a code bundle for Activity Tracker App. The original project is available at https://www.figma.com/design/odeeZHs0Lxd8IPM9BteM4D/Activity-Tracker-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  